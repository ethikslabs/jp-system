# Session Notes — 2026-03-18

## What was built today

### jp-system restructured as control layer
- CLAUDE.md rewritten — repo identity, startup sequence, execution rules
- `/projects/` folder created — live status per project
- `/secrets/` folder created — secrets registry per project
- `/health/` folder created — health check config per project
- `/people/` folder created — collaborators, prospects, partners
- `/docs/` folder created — architecture decisions, audience views
- `NORTH_STAR.md` written — the foundation document
- `THREAD_STARTER.md` written — context window protocol
- `AGENTS.md` + `README.md` already existed

### Dashboard built (jp-system/dashboard)
- Vite + React + Fastify
- Reads /projects/*.md files live
- SSE for real-time file watching
- Project cards with traffic light status
- Click → drill into project detail page
- Secrets registry with rotation warnings (90/180 day thresholds)
- Changelog — collapsed, click to expand 10 commits
- Health checks — HTTP endpoints + SSL cert expiry
- Running locally at localhost:5199
- `npm run dev` from jp-system/dashboard

### Infrastructure
- ethikslabs.com DNS updated → 13.237.68.89 (platform EC2)
- Old IP 13.238.136.164 was dead — confirmed via ping
- Cloudflare API token rotated + stored in SSM /cloudflare/api-token
- proof360 repo moved to ~/Dropbox/Projects/proof360

---

## North Star

> EthiksLabs is open source infrastructure for trust.
> A safe place to break things, iterate fast, and only ship what survives.

> The dashboard is a Founder SIEM — real time, cross-system, business and
> technical signals in one view. What Splunk does for a CISO, this does
> for a founder.

---

## The full stack

```
Research360     → data ingestion + knowledge engine  (private, forever)
Trust360        → reasoning + scoring engine         (private, forever)
         ↓
Products built on top
Proof360 / Civique / Voice pipeline / whatever survives
         ↓
ethikslabs.com       → public lab
ethikslabs.com/dashboard → Founder SIEM (Auth0 gated)
```

---

## The flywheel

```
Build in public
→ attract collaborators through the work
→ filter with $0.99 Stripe gate
→ NDA the serious ones
→ co-found the commercial version
→ hand to operator (Sarvesh model)
→ stay as architect
→ repeat
```

---

## Project lifecycle states

```
lab → public → validated → authorised → live → archived
```

Nothing moves to authorised or live without John explicitly confirming.

---

## People lifecycle

```
stranger → public visitor → $0.99 collaborator → NDA → co-founder → operator
```

---

## What still needs to be built

### Immediate (dashboard deployment)
- Auth0 app created — Single Page Application — EthiksLabs Dashboard
- Auth0 wired into frontend (React SDK)
- Fastify API protected with JWT verification
- Fastify serves built React app as static files
- Nginx config — dashboard.ethikslabs.com → localhost:PORT
- PM2 to keep it running
- Cloudflare DNS — dashboard.ethikslabs.com → 13.237.68.89

### Dashboard features (next)
- Lifecycle state per project card (lab/public/validated/authorised/live)
- Codebase health score (npm audit + CISA KEV + secrets scan)
- Audience-based views (teen/founder/CISO/board/investor/custom)
- Per-repo dashboard (proof360.au/dashboard)
- Kanban view (lifecycle columns)
- People view (who's engaging with what)

### Platform features (after)
- Public API facade — POST /api/assess
- $0.99 Stripe gate → Auth0 account → HubSpot contact
- Public lab page — ethikslabs.com/lab
- Q&A thread per project
- WhatsApp link per project
- HubSpot integration from jp-system
- Usage-based billing (Metronome)

### Observability (per repo)
- npm audit → vulnerability count + CISA KEV cross-reference
- AWS Inspector integration
- PM2 process health via SSM
- API call tracking (Firecrawl, Claude, Trust360)
- Pipeline metrics (sessions, completions, drop-off)
- Cost tracking (API spend per project)

---

## Key decisions made today

1. jp-system is the control layer + state. Repos are the source of truth for themselves.
2. Research360 + Trust360 stay private forever. Products sit on top.
3. Ethiks360 is Sarvesh's to operate. John stays architect.
4. Deal room lives in Ethiks360, not Proof360.
5. Proof360 produces the trust output. Ethiks360 presents it.
6. API facade pattern — engines never exposed, clean API surface only.
7. $0.99 as commitment filter (WhatsApp model).
8. Audience-based views — same data, Claude interprets for the lens.
9. CyberLab identified as potential standalone education product.
10. CISSP-level structure was always the foundation — SharePoint → live Xero → trust platform.

---

## For Claude chat — first pass instructions

Read this file. Then:

1. Review the north star — is anything inconsistent or missing?
2. Review "what still needs to be built" — is the order right?
3. Identify the 3 most important specs Kiro needs to write first
4. For each spec: what are the inputs, outputs, and acceptance criteria?

Do not invent architecture.
Surface gaps and sequence the work.
Output should be ready to paste into Kiro.

---

## Blockers

- Auth0 app not yet created (dashboard deployment blocked on this)
- brief-ingram.md not committed to proof360/docs (Ingram wiring blocked)
- ReachLX 10-question design not agreed (lunch with Paul Findlay pending)
